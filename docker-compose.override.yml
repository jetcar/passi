version: '3.4'

services:
  nginx:
    volumes:
      - ../passi_cert:/etc/cert:ro
      - ../passi_config/nginx/conf:/etc/nginx:ro
      - /logs:/logs



  identityserver:
    environment:
      - SessionLength=1000




